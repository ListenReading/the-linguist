// imports
var TimedText = require('./TimedText');


function LoadText() {
}

LoadText.prototype = {
  // srt: String
  // return array of TimedText entries
  parseSrt: function(srt) {
    
  },

  // csv:       Array[Array[String]]
  // timedText: Array[TimedText]
  mergeCsvAndTimedText: function(csv, timedText) {

  }
};

module.exports = LoadText;